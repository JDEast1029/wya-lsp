import { ILanguageMode } from './ILanguageMode';
import { CompletionItem } from 'vscode-languageserver-types';
import { TextDocumentPositionParams } from 'vscode-languageserver/node';

export class JSLanguageMode implements ILanguageMode {

	// doComplete(_textDocumentPosition: TextDocumentPositionParams): CompletionItem[] {
	// 	return [];
	// }
}